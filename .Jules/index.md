## 2026-01-31 - [Layout Metadata Strategy] **Learning:** In Next.js App Router, Client Components cannot export metadata. Instead of refactoring large client pages into Server/Client splits, a nested layout.tsx can be used to provide static metadata for that route and its children. **Action:** Use nested layouts for static metadata on client-heavy routes to minimize code changes and avoid breaking client-side logic.
